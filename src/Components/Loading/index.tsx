import { BarLoader } from "react-spinners";

const LoadingPage = () => {
  return (
    <div className="w-full flex items-center justify-center">
      <BarLoader color="#ffa800" />
    </div>
  );
};
export default LoadingPage;
